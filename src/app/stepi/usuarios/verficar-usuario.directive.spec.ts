import { VerficarUsuarioDirective } from './verficar-usuario.directive';

describe('VerficarUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new VerficarUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
