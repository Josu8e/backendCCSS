import { VerificarCedulaDirective } from './verificar-cedula.directive';

describe('VerificarCedulaDirective', () => {
  it('should create an instance', () => {
    const directive = new VerificarCedulaDirective();
    expect(directive).toBeTruthy();
  });
});
