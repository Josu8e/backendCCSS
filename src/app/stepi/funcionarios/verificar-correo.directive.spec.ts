import { VerificarCorreoDirective } from './verificar-correo.directive';

describe('VerificarCorreoDirective', () => {
  it('should create an instance', () => {
    const directive = new VerificarCorreoDirective();
    expect(directive).toBeTruthy();
  });
});
